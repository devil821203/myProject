"use strict";

/**
 * Popup 顯示模式設定
 *
 * mode:
 *   "user"      一般使用者介面，只顯示主要功能與簡化狀態。
 *   "developer" 開發者介面，顯示完整 CDP 狀態、間隔設定與偵錯 Log。
 */
globalThis.YOUTUBE_AUTO_SKIPPER_CONFIG = Object.freeze({
  mode: "user"
});
